[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# SPA 路由回退：/api/* 之外的路径返回 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
